import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.salescapsulas',
  appName: 'sales-capsulas',
  webDir: 'dist'
};

export default config;
